var mySwiper = new Swiper('.swiper-container', {
    // Cấu hình tùy chỉnh
    loop: true, // Lặp lại các slide
    autoplay: {
      delay: 3000, // Thời gian hiển thị mỗi slide (tính bằng mili giây)
    },
  });